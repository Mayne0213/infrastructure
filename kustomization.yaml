apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml
  - argocd/
  - ingress/ingress.yaml
  - sealed-secrets/
  # Gitea is managed manually to avoid circular dependency
  # Apply with: kubectl apply -f gitea/
  # - gitea/
