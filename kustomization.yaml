apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml
  - argocd/
  - ingress/ingress.yaml
  - gitea/  # Backup jobs and runner (Gitea Application itself is managed manually)
  # Gitea Application is managed manually to avoid circular dependency
  # Apply with: kubectl apply -f argocd/applications/dev-tools/gitea.yaml
